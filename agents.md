You are working inside of a github repo. Commit to main without PR. Each push will be deployed automatically on a github page.

Documentation rules:

- When the user requests a change that affects product behavior, features, or scope, update `concept.md` in the same change.
- Keep `concept.md` focused on product goals and requirements. Document technical decisions and implementation details in `architecture.md`.
- Update `architecture.md` whenever the technical design changes.
- Keep `concept.md`, `architecture.md`, and the implementation consistent. If they conflict, resolve the documentation as part of the change.
